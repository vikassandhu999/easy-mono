// Re-export all nutrition utilities from @easy/utils for backward compatibility
export {
  computeMacrosFromSnapshot,
  computePlannedMacros,
  formatDateDisplay,
  formatDateISO,
  formatMacroValue,
  getWeekdayFromDate,
  type MacroTotals,
  MEAL_SLOT_LABELS,
  MEAL_SLOTS,
  type MealSlot,
  normalizeMacros,
  sumMacros,
  WEEKDAY_LABELS,
  WEEKDAY_SHORT_LABELS,
  WEEKDAYS,
} from '@easy/utils';
